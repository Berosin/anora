# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# --- Document extraction ---
PyMuPDF==1.24.11        # PDF text + page extraction
python-docx==1.1.2      # DOCX paragraph/section extraction

# --- Validation ---
pydantic>=2.13.0

# --- Testing ---
pytest==8.3.3
httpx==0.27.2            # required by FastAPI's TestClient

# --- Embeddings + vector database (Phase 5) ---
sentence-transformers==3.1.1   # real embedding model — not installed in this
                                # sandbox (no network access to Hugging Face
                                # to download weights); works normally with
                                # internet access. See app/embeddings/embedder.py.
qdrant-client==1.11.2
